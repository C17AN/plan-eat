import styled from "@emotion/styled";

type Props = {};

const SendOpinion = (props: Props) => {
  return <Container>의견 보내기</Container>;
};

const Container = styled.div`
  position: fixed;
  bottom: 20px;
  left: 20px;
`;

export default SendOpinion;
